model:
  type: "Joeyllm"
  vocab_size: 50257
  max_seq_len: 64
  embed_dim: 768
  num_heads: 8
  num_layers: 4
  dropout: 0.1
  batch_size: 4
  device: "auto"

huggingface:
  dataset_name: "SouthernCrossAI/Gutenberg-Australia-Chunks-512"
  columns: ["input_ids"]
  format: "torch"
  shuffle: true
  use_validation: true
  use_test: true

